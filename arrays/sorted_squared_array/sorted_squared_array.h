#ifndef SORTED_SQUARED_ARRAY
#define SORTED_SQUARED_ARRAY
#include <vector>

std::vector<int> sortedSquaredArraySolution1(std::vector<int> array);

std::vector<int> sortedSquaredArraySolution2(std::vector<int> array);

#endif